---
phase: 07-map-validation
task: 5
total_tasks: 6
status: in_progress
last_updated: 2026-03-07T01:46:22.097Z
---

<current_state>
Session 9: Three commits (437ead5→541ff36) fixing wake sequence regression and
related issues. Wake burst restored, is_docked fixed for UNKNOWN status, deep
sleep wake coordination fixed. Robot is awake and broadcasting STANDBY/docked=True
but Start command not yet validated (user couldn't test — entity was unavailable
from earlier bug, now fixed in 541ff36).
</current_state>

<completed_work>

**Session 9 — 3 commits (437ead5 → 541ff36):**

1. **437ead5 — Restored get_device_base_status to wake burst**
   - Root cause: commit a110cef removed all query commands from wake burst
   - Passive commands (subscribe/heartbeat) only wake WS server, not main CPU
   - get_device_base_status forces robot into command-ready state
   - Removed broken get_status() priming step from _ensure_awake()
   - Fixed wake() reconnect guard (removed _listener_active gate — later reverted)

2. **fd0216c — is_docked handles UNKNOWN status + one-time status fetch**
   - is_docked now checks dock fields (f11, f47) for ANY non-cleaning status
   - Previously only checked for STANDBY/DOCKED/CHARGED → False when UNKNOWN
   - Added _fetch_initial_status() background task when broadcasts lack field 3
   - Broadcasts reset poll timer → normal poll never fires while broadcasting
   - "Self test paused" is an unmapped working_status value (robot on dock)

3. **541ff36 — Prevent wake/keepalive interference + entity stays available**
   - Restored _listener_active guard on wake() reconnect
   - wake() reconnect races with listener loop, resets keepalive failure counter
   - Robot enforces single-connection-per-IP ("close old one" error confirmed)
   - Reduced _WAKE_RECONNECT_THRESHOLD from 4→2 (30s instead of 60s)
   - Coordinator poll returns stale data when robot asleep (no more unavailable)

</completed_work>

<remaining_work>

**Immediate — deploy and validate:**
- Deploy 541ff36, restart HA
- Wait for robot to sleep, confirm wake works (keepalive escalates to reconnect after 30s)
- Confirm entity stays available when robot sleeps
- Press Start when robot awake — validate fire-and-forget clean command works
- If robot starts: verify CLEANING broadcast, trail rendering
- If robot doesn't start: sniff Narwal app clean/plan/start payload

**Still pending from prior sessions:**
- Fix trail: segment breaks on large jumps (>1m grid distance)
- 07-03 plan: post-cleaning map refresh (_was_cleaning + _refresh_map_after_clean)
- Investigate display_map dropout during cleaning (~90s gaps)

</remaining_work>

<decisions_made>

- get_device_base_status MUST stay in wake burst — it forces active CPU wake (a110cef removal caused 5-commit regression)
- wake() must NOT reconnect when listener is active — causes race with keepalive and "close old one" from robot
- Only keepalive loop should do deep-sleep WS reconnect (closes WS, listener reconnects)
- is_docked should check dock fields for any non-cleaning status, not just STANDBY
- Entity should stay available when robot sleeps — return stale data, not UpdateFailed
- Broadcasts reset poll timer → need _fetch_initial_status() for field 3 when broadcasts lack it

</decisions_made>

<blockers>

- Start command still unvalidated — fire-and-forget removes timeout error but robot may not start (payload issue?)
- Deep sleep wake: improved (faster escalation, no interference) but still untested with 541ff36
- "Self test paused" is unmapped working_status — shows UNKNOWN until get_status() fires

</blockers>

<context>
Git HEAD: 541ff36. Both narwal_client/ copies IN SYNC.
Key files changed: client.py (_build_wake_commands, wake, _keepalive_loop),
  vacuum.py (_ensure_awake), coordinator.py (_async_update_data, _fetch_initial_status),
  models.py (is_docked).
The wake system has been a recurring regression area. Key invariant documented in MEMORY.md:
  NEVER remove get_device_base_status from wake burst.
  wake() must NOT reconnect when _listener_active (keepalive handles it).
</context>

<next_action>
1. Deploy 541ff36, restart HA
2. Let robot go to sleep (~60s idle), watch keepalive escalate to WS reconnect after 30s
3. Confirm robot wakes and entity stays available throughout
4. Press Start — validate fire-and-forget start command
5. If start works → fix trail segments → plan 07-03
6. If start fails → capture Narwal app's clean/plan/start payload via packet sniff
</next_action>
