---
phase: 11-vision-obstacles
task: 2
total_tasks: 2
status: blocked
last_updated: "2026-03-15T12:06:13.824Z"
---

<current_state>
Phase 11 remains blocked on vision obstacle data source. This session was focused on GitHub issue triage and bug fixes rather than Phase 11 work. Three critical bugs were fixed, community issues responded to, and a config flow connection failure affecting multiple users was investigated and partially fixed.
</current_state>

<completed_work>

- fix(Segment import): conditional import of HA 2026.3 Segment class — integration now loads on all HA versions (commit 502d3d0)
- fix(CI): added retry for hassfest Docker image pull to handle GHCR rate limits (commit 0fbba0e)
- fix(config flow): drain stale WebSocket field5 responses between discover and get_device_info — prevents stale wake probe responses from being consumed as real responses (commit b6a5405)
- fix(config flow): added exception logging — setup failures now log actual exception type and message instead of silently swallowing
- Responded to all open GitHub issues (#4, #5, #7, #8) with analysis, fix announcements, and debug log collection instructions
- Analyzed debug logs from TWiesendanger (Flow) and jfreak80 (Flow, FW 1.02.19.02) — both show same pattern: discovery succeeds but get_device_info disconnects 22ms later
</completed_work>

<remaining_work>

- Phase 11 Task 2: Vision obstacle data source investigation — STILL BLOCKED (see previous .continue-here.md for full context)
- Plan 11-02: VisionObstacleInfo model, parsing, overlay rendering — NOT STARTED
- Issue #8 follow-up: await user test results with new diagnostic logging to confirm stale response fix works or reveal actual exception
- Issue #8 deep sleep wake: some Flows don't respond to wake commands from deep sleep — firmware-dependent, needs investigation (user's Flow wakes fine, others don't)
</remaining_work>

<decisions_made>

- Segment import uses try/except guard instead of requiring HA 2026.3 minimum version — broader compatibility
- CLEAN_AREA feature flag conditionally set based on Segment availability
- Config flow exception handling changed from silent swallow to logged warning — essential for remote debugging
- drain_ws_buffer() uses 50ms timeout per message — fast enough to not delay setup, long enough to catch buffered responses
- Freo X Ultra (#4) confirmed cloud-only (Tuya/Alibaba) by ManivannanBA's deep MITM analysis — not supportable via local WebSocket
</decisions_made>

<blockers>
- Phase 11 BLOCKED: vision obstacle data source not found (get_vision_image returns NOT_APPLICABLE, display_map has no vision fields)
- Issue #8: stale response fix pushed but awaiting user confirmation. If it doesn't fix, the new exception logging will reveal actual cause.
- Deep sleep wake failure on some Flow units — possibly firmware-dependent, need user's FW version for comparison
</blockers>

<context>
## GitHub Issue Status

| Issue | Title | Status | Action |
|-------|-------|--------|--------|
| #4 | Freo X Ultra | OPEN | Cloud-only confirmed, ZMQ port is factory debug. jack3308 exploring Tuya cloud dump |
| #5 | Z Ultra | OPEN | Product key not found. Awaiting user retest with discovery during active clean |
| #7 | Flow connect | OPEN | Segment fix deployed. Some users may have separate issue (same as #8) |
| #8 | No Connect | OPEN | Stale response drain + exception logging pushed. Awaiting test results from TWiesendanger, hecho83, jfreak80 |

## Key Finding from Issue Analysis
The 22ms disconnect after get_device_info is the core mystery for #8. Three theories:
1. **Stale field5 responses** (fix pushed) — wake probes leave responses in WS buffer that get consumed instead of real response
2. **Firmware-dependent command rejection** — malformed wake probe topics (empty device_id → double slash) may cause stricter firmwares to close connection
3. **Parsing edge case** — stale response with unexpected data structure causes downstream failure

User confirmed their Flow runs both app + HA simultaneously, so concurrent connection theory is WRONG.
</context>

<next_action>
Priority order:
1. **Check issue #8 for user test results** — if stale response fix worked, close the issue. If not, the new exception logging will show exactly what fails.
2. **If #8 still failing**: implement passive-first discovery (listen for broadcasts WITHOUT sending wake commands first, only send commands after device_id is known)
3. **Phase 11**: resume vision obstacle investigation per previous .continue-here.md (run map_field_decoder.py --save while idle, probe_all_topics.py during fresh clean)
</next_action>
